source .venv/bin/activate
.venv/bin/pip install --upgrade build
.venv/bin/python3 -m build

# limpa
find . -name '*.pyc' -delete
rm -rf .pytest_cache dist build

# 
for f in /Users/sergiosousa/TA.support/src/TA/support/domain/entities/*.py; do
  echo "===== $f =====" >> resultado.txt
  cat "$f" >> resultado.txt
  echo "" >> resultado.txt
done

find src -type f -name '*.py' -exec sh -c 'echo "# Arquivo: {}"; cat {}; echo; echo' \; > codigo_python.txt